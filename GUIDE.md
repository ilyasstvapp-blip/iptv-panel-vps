# 🎯 دليل الاستخدام السريع - IPTV Admin Dashboard

## 📋 نظرة عامة

تم إنشاء لوحة تحكم IPTV احترافية كاملة باستخدام:
- ✅ HTML5 النقي
- ✅ CSS3 مع Animations
- ✅ JavaScript (Vanilla JS)
- ✅ بدون أي Frameworks

---

## 🚀 البدء السريع

### 1. فتح اللوحة

يمكنك فتح اللوحة بطريقتين:

**أ) محلياً (للتجربة):**
```
file:///c:/Users/USER/Desktop/iptv-panel-vps/index.html
```

**ب) على السيرفر (للإنتاج):**
```
http://your-server-ip/iptv-panel-vps/
```

### 2. الوضع التجريبي (Demo Mode)

اللوحة تعمل حالياً في **وضع التجربة** مع بيانات وهمية:
- ✅ جميع الأزرار تعمل
- ✅ جميع الصفحات نشطة
- ✅ التصميم كامل ومتجاوب
- ⚠️ البيانات محفوظة محلياً فقط (لا يوجد Backend حقيقي)

---

## 📱 الصفحات والوظائف

### 🏠 الصفحة الرئيسية (Dashboard)

**ماذا تعرض:**
- عدد القنوات الكلي
- عدد القنوات المباشرة (LIVE)
- عدد القنوات المتوقفة (OFF)

**الوظائف:**
1. **زر "إضافة قناة جديدة"**
   - اضغط عليه لإظهار نموذج الإضافة
   - املأ البيانات:
     - اسم القناة (مثال: BeIN Sports 1)
     - المجموعة (BeIN SPORT / SSC / OSN / OTHER)
     - نوع الرابط (m3u8 / ts)
     - رابط القناة (URL كامل)
   - اضغط "حفظ وتشغيل"
   - ستتم إضافة القناة فوراً

---

### 📺 صفحة القنوات (Channels)

**ماذا تعرض:**
- القنوات مجمعة حسب الباقات (Groups)
- كل باقة قابلة للتوسيع والطي

**الوظائف لكل قناة:**

| الزر | الوظيفة | الشرح |
|------|---------|-------|
| ▶ Play | تشغيل القناة | يفتح مشغل فيديو داخل نافذة منبثقة |
| 📋 Copy | نسخ الرابط | ينسخ رابط البث إلى الحافظة |
| ⏹ Stop | إيقاف القناة | يوقف البث ويغير الحالة إلى OFF |
| ▶ Start | تشغيل القناة | يشغل البث ويغير الحالة إلى LIVE |
| ✏ Edit | تعديل القناة | يفتح نموذج التعديل مع البيانات الحالية |
| 🗑 Delete | حذف القناة | يطلب تأكيد ثم يحذف القناة نهائياً |

**كيفية الاستخدام:**
1. اضغط على اسم الباقة لتوسيعها
2. ستظهر جميع القنوات التابعة لها
3. استخدم الأزرار حسب الحاجة

---

### 📊 صفحة الإحصائيات (Statistics)

**ماذا تعرض:**
- إجمالي القنوات
- القنوات النشطة
- الجلسات النشطة (Sessions)
- حالة السيرفر (Online/Offline)
- رسم بياني للبيانات

**التحديث التلقائي:**
- يتم تحديث البيانات كل 5 ثواني تلقائياً
- يمكن إيقاف التحديث من صفحة الإعدادات

---

### ⚙️ صفحة الإعدادات (Settings)

**الخيارات المتاحة:**

1. **اللغة (Language)**
   - العربية (Arabic)
   - English
   - التبديل فوري بدون Reload

2. **الوضع الليلي (Dark Mode)**
   - تشغيل/إيقاف
   - يتم حفظ الاختيار تلقائياً

3. **التحديث التلقائي (Auto Refresh)**
   - تشغيل/إيقاف
   - يحدث البيانات كل 5 ثواني

**ملاحظة:** جميع الإعدادات تُحفظ في `localStorage` وتبقى حتى بعد إغلاق المتصفح.

---

## 🔌 ربط Backend PHP

### المتطلبات:
- PHP 7.4 أو أحدث
- MySQL / MariaDB
- FFmpeg (لإدارة البث)
- Web Server (Apache / Nginx)

### خطوات الربط:

#### 1. إنشاء قاعدة البيانات

```sql
CREATE DATABASE iptv;

USE iptv;

CREATE TABLE channels (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE,
    `group` VARCHAR(100) NOT NULL,
    url TEXT NOT NULL,
    type ENUM('m3u8', 'ts') DEFAULT 'm3u8',
    status ENUM('live', 'offline') DEFAULT 'offline',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 2. تعديل ملفات API

افتح كل ملف في مجلد `/api/` وقم بـ:

**أ) إزالة التعليقات عن كود قاعدة البيانات:**
```php
// قبل:
// $db = new PDO('mysql:host=localhost;dbname=iptv', 'username', 'password');

// بعد:
$db = new PDO('mysql:host=localhost;dbname=iptv', 'your_username', 'your_password');
```

**ب) إزالة التعليقات عن استعلامات SQL:**
```php
// قبل:
/*
$stmt = $db->prepare("SELECT * FROM channels");
$stmt->execute();
*/

// بعد:
$stmt = $db->prepare("SELECT * FROM channels");
$stmt->execute();
```

#### 3. إعداد FFmpeg

تأكد من تثبيت FFmpeg:
```bash
# Ubuntu/Debian
sudo apt install ffmpeg

# CentOS/RHEL
sudo yum install ffmpeg
```

#### 4. إنشاء مجلد البث

```bash
mkdir -p /var/www/html/streams
chmod 755 /var/www/html/streams
```

#### 5. اختبار API

```bash
# اختبار جلب القنوات
curl http://localhost/api/channel_status.php

# اختبار إضافة قناة
curl -X POST http://localhost/api/channel_add.php \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Channel","group":"OTHER","type":"m3u8","url":"https://example.com/stream.m3u8"}'
```

---

## 🎨 التخصيص

### تغيير الألوان:

افتح `css/style.css` وعدّل المتغيرات:

```css
:root {
    /* الألوان الأساسية */
    --accent-primary: #6c5ce7;  /* لون رئيسي */
    --success: #00b894;         /* لون النجاح */
    --danger: #ff7675;          /* لون الخطر */
    
    /* الخلفيات */
    --bg-primary: #0f0f23;
    --bg-secondary: #1a1a2e;
}
```

### إضافة مجموعات جديدة:

افتح `index.html` وأضف خيار جديد في القائمة:

```html
<select id="channelGroup">
    <option value="BeIN SPORT">BeIN SPORT</option>
    <option value="SSC">SSC</option>
    <option value="OSN">OSN</option>
    <option value="YOUR_GROUP">YOUR GROUP</option> <!-- جديد -->
</select>
```

---

## 📱 Responsive Design

اللوحة متجاوبة بالكامل:

- **Desktop (1024px+):** عرض كامل مع Sidebar
- **Tablet (768-1023px):** عرض متوسط
- **Mobile (<768px):** Sidebar قابلة للسحب

**للتجربة:**
- افتح اللوحة في المتصفح
- اضغط F12 لفتح Developer Tools
- اضغط Ctrl+Shift+M للوضع المتجاوب
- جرب أحجام مختلفة

---

## 🔒 الأمان (مهم جداً)

### قبل النشر على الإنتاج:

1. **إضافة Authentication:**
```php
// في بداية كل ملف API
session_start();
if (!isset($_SESSION['admin_logged_in'])) {
    http_response_code(401);
    echo json_encode(['error' => 'Unauthorized']);
    exit;
}
```

2. **استخدام HTTPS:**
```apache
# في .htaccess
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

3. **Validate المدخلات:**
```php
$name = filter_var($data['name'], FILTER_SANITIZE_STRING);
$url = filter_var($data['url'], FILTER_VALIDATE_URL);
```

4. **استخدام Prepared Statements:**
```php
$stmt = $db->prepare("SELECT * FROM channels WHERE name = ?");
$stmt->execute([$name]);
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: اللوحة لا تعرض البيانات

**الحل:**
1. افتح Console في المتصفح (F12)
2. تحقق من الأخطاء
3. تأكد من أن ملفات API موجودة
4. تحقق من صلاحيات الملفات

### المشكلة: الأزرار لا تعمل

**الحل:**
1. تأكد من تحميل ملف `js/app.js`
2. افتح Console وابحث عن أخطاء JavaScript
3. تحقق من المسارات في الكود

### المشكلة: التصميم غير صحيح

**الحل:**
1. تأكد من تحميل ملف `css/style.css`
2. تحقق من تحميل خط Cairo من Google Fonts
3. امسح Cache المتصفح (Ctrl+Shift+Delete)

---

## 📞 الدعم الفني

### الملفات المهمة:

| الملف | الوظيفة |
|-------|---------|
| `index.html` | الصفحة الرئيسية |
| `css/style.css` | جميع الأنماط |
| `js/app.js` | جميع الوظائف |
| `api/*.php` | نقاط الاتصال بالـ Backend |

### للمساعدة:
1. راجع ملف `README.md`
2. افحص Console في المتصفح
3. تحقق من Network Tab لرؤية طلبات API

---

## ✅ Checklist قبل النشر

- [ ] تم إنشاء قاعدة البيانات
- [ ] تم تعديل بيانات الاتصال بقاعدة البيانات
- [ ] تم تثبيت FFmpeg
- [ ] تم إنشاء مجلد `/streams/`
- [ ] تم اختبار جميع API endpoints
- [ ] تم إضافة Authentication
- [ ] تم تفعيل HTTPS
- [ ] تم اختبار اللوحة على أجهزة مختلفة
- [ ] تم عمل Backup لقاعدة البيانات

---

## 🎉 الخلاصة

لديك الآن لوحة تحكم IPTV احترافية كاملة:

✅ تصميم عالمي حديث  
✅ جميع الوظائف تعمل  
✅ جاهزة للربط مع Backend  
✅ متجاوبة بالكامل  
✅ Dark/Light Mode  
✅ دعم العربية والإنجليزية  
✅ بدون أي Frameworks  

**استمتع بالاستخدام! 🚀**
